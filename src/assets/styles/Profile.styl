.profile__container
  width 100vw
  height 100vh
  display grid
  grid-template-columns repeat(5, 1fr)
  grid-template-rows repeat(10, 1fr)

.profile__photo
  position fixed
  width 100%
  height 100%
  object-fit cover
  z-index -10