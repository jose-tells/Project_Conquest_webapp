.reel
  position fixed
  height 100vh
  z-index -100
  filter grayscale()

.wrap_container
  height 90vh
  overflow hidden
  height 91vh
  display grid
  grid-template-columns repeat(5, 1fr)
  grid-template-rows repeat(10, 1fr)
